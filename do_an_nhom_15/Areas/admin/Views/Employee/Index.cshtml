@model do_an_nhom_15.Models.ViewModel

<div class="container mt-5">
    <!-- Add New Employee Button -->
    <div class="text-end mb-3">
        <a class="btn btn-primary" href="/admin/Employee/Add">
            <i class="fas fa-user-plus"></i> Add New Employee
        </a>
    </div>

    @if (Model.EmployeeList != null && Model.EmployeeList.Any())
    {
        <div class="row row-cols-1 row-cols-md-2 g-4">
            @foreach (var item in Model.EmployeeList)
            {
                <div class="col">
                    <div class="rounded bg-secondary h-100 ">
                        <div class="card-body p-4">
                            <h5 class="card-title">
                                <i class="fas fa-user text-white"></i> @item.FirstName @item.LastName
                            </h5>
                            <p class="card-text"><strong>Position:</strong> @item.Position</p>
                            <p class="card-text"><strong>Email:</strong> @item.Email</p>
                            <p class="card-text"><strong>Phone:</strong> @item.PhoneNumber</p>
                            <p class="card-text"><strong>Salary:</strong> @item.Salary</p>
                            <p class="card-text"><strong>Status:</strong> @item.Status</p>
                        </div>
                        <form class="card-footer text-end border-top" asp-action="Remove" asp-route-id="@item.EmployeeId" method="post" onsubmit="return confirm('Are you sure you want to delete this employee?');">
                            <a href="/admin/Employee/Edit/@item.EmployeeId" class="btn btn-warning me-2">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="fas fa-trash-alt"></i> Delete
                            </button>
                        </form>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <p class="text-center">No employees found.</p>
    }
</div>
